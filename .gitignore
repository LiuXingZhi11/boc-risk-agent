# Local secrets
.env
.streamlit/secrets.toml

# Python caches and test output
__pycache__/
*.py[cod]
.pytest_cache/

# Local databases and generated runtime data
/data/

# Source materials are project inputs and may be versioned with the project.
# Keep local model files and secrets out of Git through the rules above.

# Editor and operating-system files
.vscode/
.DS_Store
Thumbs.db
