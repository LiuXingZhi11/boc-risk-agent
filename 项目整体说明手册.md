# 科技型企业风险辅助审查系统

## 项目整体说明手册

本文档面向项目开发人员、业务人员和外部集成方，用于说明项目要解决的问题、主要代码结构、数据流、模型调用方式、风险评级逻辑以及 MCP/Skill 扩展方式。

本文档是项目总览，不替代具体业务规则手册、本体维护手册和 MCP/Skill 平台手册。

---

## 一、项目定位

本项目用于辅助分析科技型企业的公开材料，主要材料包括：

- 企业年度报告；
- 招股说明书；
- 企业公告和其他 HTML 材料；
- 行业研究报告；
- 可选的外部 MCP 数据。

系统将材料转化为可追溯的证据切片，在证据基础上生成：

1. 企业画像；
2. 行业背景画像；
3. 单个风险评级方向报告；
4. 企业客户风险评级报告；
5. 后续需要人工关注的行动建议。

系统定位是“风险信息整理和分析辅助工具”，不自动作出授信决定，也不能替代人工尽职调查、合规审查和最终审批。

---

## 二、整体处理链路

```text
PDF / HTML / 外部数据
        ↓
SourceAsset（原始材料）
        ↓
EvidenceUnit（证据切片）
        ↓
ReAct Agent 检索、读取和补充证据
        ↓
结构化事实候选
        ↓
Python 证据校验、字段校验、去重和归并
        ↓
企业画像 / 行业背景画像
        ↓
风险评级方向分析
        ↓
Python 按方向权重计算总分
        ↓
客户风险评级报告和行动建议
```

系统中同时存在两类信息：

- 结构化信息：材料、证据切片、事实、关系、画像条目、评分和数据库记录；
- 自然语言信息：企业画像分析、行业分析、风险判断、评级说明和行动建议。

结构化信息负责约束和追溯，自然语言负责解释和表达。模型不能绕过证据直接生成正式结论。

---

## 三、核心数据对象

### 1. SourceAsset：原始材料

记录一份 PDF、HTML 或其他材料的基本信息，包括：

- 材料名称；
- 企业或行业归属；
- 报告期；
- 来源类型；
- 文件路径或来源地址。

### 2. EvidenceUnit：证据切片

`EvidenceUnit` 是系统最重要的证据对象。每个切片保存：

- 切片 ID；
- 原始材料 ID；
- 企业案例 ID；
- 页面或 HTML 位置；
- 标题和章节路径；
- 切片正文；
- 报告期；
- 切片顺序和相邻关系。

PDF 默认按照章节、表格和页面内容切分，单个正文切片上限由 `src/sources/pdf_chunker.py` 控制，目前默认约为 3200 个字符。系统保留同章节相邻切片的关联，便于处理跨页表格和连续段落。

### 3. 结构化事实

结构化事实是模型从证据中提取的事实候选，例如：

- 企业拥有某项技术；
- 企业与某客户存在销售关系；
- 企业某年度营业收入为某金额；
- 某人员担任董事或核心管理职务。

事实必须带有证据引用，不能只保存模型概括后的句子。

### 4. 企业画像

企业画像由事实、关系和主题分析组成。当前画像重点覆盖：

- 企业与控制；
- 企业治理与团队；
- 技术与知识产权；
- 产品与项目；
- 市场与商业化；
- 客户与供应商；
- 财务与融资；
- 风险事项；
- 权威认定；
- 结果与处置。

画像候选先经过 Python 校验，再由 DeepSeek 对同一方向的事实进行主题归纳和分析。

### 5. 行业背景画像

行业画像主要包括八个维度：

- 行业发展阶段；
- 市场规模与增长；
- 技术路线；
- 产业链和价值链；
- 竞争格局；
- 商业化；
- 政策与监管；
- 行业风险。

行业画像的作用不是代替企业画像，而是提供行业基准，用于判断企业在某个风险评级方向上的位置和表现。

### 6. 风险评级报告

风险评级报告包括：

- 11 个风险评级方向；
- 每个方向的分析结论；
- 每个方向的得分；
- 使用的企业事实和行业背景；
- 信息不足和需要核实的事项；
- 各方向得分合计后的总分；
- 综合风险判断和行动建议。

总分由 Python 计算，模型负责解释得分依据，不负责自行相加或篡改总分。

---

## 四、代码目录说明

### 根目录

| 文件或目录 | 作用 |
|---|---|
| `v5_app.py` | Streamlit 前端入口 |
| `README.md` | 项目快速说明和启动方式 |
| `项目整体说明手册.md` | 本总览手册 |
| `requirements.txt` | Python 依赖 |
| `config/` | 模型、MCP 和 Skill 配置 |
| `data/` | SQLite 数据库和运行数据 |
| `materials/` | 企业报告、行业报告和参考材料 |
| `prompts/` | 运行时提示词 |
| `skills/` | 可插拔 Skill 定义 |
| `docs/` | 技术和业务专项手册 |

### `src/sources/`

负责 PDF、HTML 和其他来源材料的解析，并生成 `SourceAsset` 与 `EvidenceUnit`。

主要模块：

- `pdf_adapter.py`：PDF 读取入口；
- `pdf_chunker.py`：章节、表格和页面切片；
- `html_adapter.py`：HTML 材料转换；
- `registry.py`：材料注册。

### `src/evidence/`

负责证据的保存、查询、关键词检索、相邻切片读取和外部证据追踪。

内部材料使用 `EvidenceUnit`，外部 MCP 结果使用独立的 `ExternalEvidenceTrace`，二者不直接混为一谈。

### `src/profiles/`

负责企业画像流程：

- `react_workflow.py`：单个企业、单个领域的 ReAct 调查；
- `react_tools.py`：搜索和读取证据工具；
- `extraction.py`：结构化事实候选提取；
- `candidates.py`：候选校验、证据绑定、去重和归并；
- `topic_analysis.py`：企业画像主题分析；
- `repository.py`：画像数据入库。

### `src/industry/`

负责行业背景画像：

- `react_workflow.py`：行业证据检索；
- `extraction.py`：行业洞察提取；
- `topic_analysis.py`：行业主题分析；
- `repository.py`：行业画像入库。

行业 ReAct 默认允许读取比单企业画像更多的行业材料，因为行业报告通常需要覆盖多个维度。

### `src/approval/`

负责风险评级：

- `guideline_definitions.py`：11 个评级方向、审批点、约束等级和分值权重；
- `context.py`：企业画像与行业画像上下文拼装；
- `reporting.py`：单方向风险报告；
- `direction_ranking.py`：同方向企业比较和排名；
- `overall_assessment.py`：企业总分和综合评价；
- `action_recommendations.py`：行动建议；
- `repository.py`：评级结果入库。

当前 11 个方向的权重合计为 100 分，具体权重以 `guideline_definitions.py` 为准。权重变化应先同步业务手册和测试，再修改代码。

### `src/llm/`

负责 DeepSeek 调用：

- `deepseek_client.py`：Chat Completions 调用和 JSON 解析；
- `generation_config.py`：sampling、thinking、token 和重试参数。

模型配置统一位于：

```text
config/model_config.yaml
```

当前默认模型为 `deepseek-v4-flash`。事实提取通常使用 `sampling`，检索、主题分析、风险分析和报告生成使用 `thinking`。

### `src/ontology/` 与 `prompts/data/`

本体定义字段、关系、对象类型、信息状态和内容角色。提示词中的数据规则负责告诉模型：

- 字段表示什么；
- 哪些表述可以归入该字段；
- 如何识别同义表达；
- 哪些内容不能臆造；
- 如何绑定证据。

本体 YAML 负责结构约束，Markdown 提示词负责理解规则，两者共同作用。

### `src/platform/`

这是可选的 MCP 和 Skill 扩展层，不承载企业画像和风险评级的核心业务逻辑。

- `src/platform/mcp/`：MCP Provider、工具发现、工具解析、调用限制、超时和审计；
- `src/platform/skills/`：Skill 加载、Schema 校验、启用、解析和 Agent 注入。

---

## 五、模型调用和 Python 代码的分工

### DeepSeek 负责

- 阅读证据切片；
- 判断哪些切片与当前领域相关；
- 从证据中提取事实候选；
- 对事实进行主题归纳；
- 结合企业画像和行业背景写风险分析；
- 生成综合判断和行动建议。

### Python 负责

- 读取和切分材料；
- 提供检索、读取和相邻证据工具；
- 校验模型输出的 JSON 结构；
- 检查字段、关系、事实引用是否合法；
- 去重、归并和处理冲突；
- 保存数据库记录；
- 计算 11 个方向的总分；
- 生成排名、汇总结果和界面展示。

模型可以做理解和表达，但不能因为自然语言输出而改变证据、字段定义或总分计算规则。

---

## 六、企业画像的 ReAct 流程

企业画像不是一次把所有 PDF 内容发送给模型，而是按领域运行。

单个领域的基本流程是：

1. Python 提供当前案例、领域目的和证据目录；
2. Agent 先看到证据标题、章节和元数据；
3. Agent 根据领域目的选择需要读取的切片；
4. Agent 通过 `read_evidence` 读取正文；
5. 达到轮次或读取上限后，模型输出结构化事实候选；
6. Python 校验候选并保存为待审核结果；
7. 对拒绝、遗漏或关系不完整的候选执行有限补充检索；
8. 主题分析阶段再把该领域经过校验的事实和证据传给模型，生成画像分析。

因此，一个企业的多个画像领域需要分别运行。行业背景画像也使用 ReAct，但会按照行业维度批量检索多份行业报告。

---

## 七、风险评级流程

风险评级以单个企业为中心，不要求必须存在同行样本。

每个评级方向的输入通常包括：

- 企业画像中的相关事实和主题分析；
- 行业背景画像中的对应维度；
- 评级方向定义和审批点；
- 相关证据引用；
- 可选的同行比较结果。

模型输出：

- 该方向的分析摘要；
- 各审批点判断；
- 方向得分；
- 证据不足和需要核实的事项；
- 风险说明。

Python 再根据方向定义中的权重计算总分。同行样本存在时，可以增加排名和相对位置；没有同行样本时，不阻止单企业评级报告生成。

外部材料或 MCP 结果如果没有经过内部证据化和人工确认，不能直接作为正式评级结论。

---

## 八、MCP 和 Skill 扩展

### 1. MCP 是什么

MCP 是外部工具的连接层。例如企查查可以通过 MCP 提供工商、股权、司法、知识产权等查询工具。

配置文件：

```text
config/mcp_servers.yaml
```

密钥通过环境变量读取，例如：

```powershell
$env:QCC_API_KEY="你的企查查密钥"
$env:MCP_ENABLED="true"
$env:SKILLS_ENABLED="true"
```

### 2. Skill 是什么

Skill 是面向某个业务任务的可插拔能力包，至少包括：

```text
skills/<skill_id>/skill.yaml
skills/<skill_id>/SKILL.md
```

`skill.yaml` 声明适用 Agent、允许调用的 MCP Tool、工具次数和超时限制；`SKILL.md` 描述模型的业务使用规则。

Skill 不能直接修改 MCP 客户端、数据库 Schema 或本体，也不能调用未在 manifest 中声明的工具。

### 3. 企查查接入边界

企查查返回结果默认记录为外部证据追踪信息，不会自动写入正式 `EvidenceUnit`、企业画像或评级结论。这样可以避免外部接口错误直接污染正式结果。

当前 `skills/examples/qcc_demo/` 只是平台演示，使用的是 Fake Provider，不是正式企查查 Skill。正式接入时必须替换为企查查提供的实际 MCP 地址和工具名。

---

## 九、配置和启动

安装依赖：

```powershell
pip install -r requirements.txt
```

填写 `config/model_config.yaml` 中的模型配置，启动界面：

```powershell
streamlit run v5_app.py
```

运行测试：

```powershell
python -m pytest -q -p no:cacheprovider
```

MCP/Skill 默认关闭。只有明确配置环境变量、启用 MCP Server 和启用 Skill 后，Agent 才会加载外部工具。

---

## 十、开发和维护规则

1. 新增业务字段，先修改本体和数据提示词，再修改 Python 校验代码。
2. 新增评分方向或调整权重，必须同步 `guideline_definitions.py`、业务手册和测试。
3. 新增 Skill，优先只新增 `skills/<skill_id>/` 和配置，不直接修改核心 Agent。
4. 所有模型结论必须保留证据引用或明确标注信息不足。
5. 外部 MCP 数据和内部 EvidenceUnit 分开保存。
6. 不把真实 API Key 提交到公开仓库或业务提示词中。
7. 先运行离线测试，再进行真实模型或真实 MCP 调用。

---

## 十一、专项手册索引

- 技术架构和目标：`docs/project/科技型企业风险辅助审查系统_当前与目标技术手册.md`
- 项目交接和运行入口：`docs/project/PROJECT_HANDOFF.md`
- 本体设计和维护：`docs/project/本体设计与维护手册.md`
- 客户风险评级流程：`docs/business/客户风险评级报告生成工作手册.md`
- MCP/Skill 平台接入：`boc-risk-agent_MCP_Skill_Extension_Platform_Manual.md`
- MCP 运行层：`src/platform/mcp/README.md`
- Skill 运行层：`src/platform/skills/README.md`

