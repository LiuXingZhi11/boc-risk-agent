# PROJECT HANDOFF

> 当前状态日期：2026-08-11  
> 本文件只记录当前有效状态和下一步，不记录历史实现过程。

## 1. 阅读顺序

1. [当前与目标技术手册](科技型企业风险辅助审查系统_当前与目标技术手册.md)
2. [Ontology 设计规范](../../ontology/科技型企业通用Ontology设计规范_精简版_V0.8.md)
3. [README](../../README.md)

## 2. 当前业务目标

系统从企业材料、同行企业年报、行业报告和历史案例中提取有证据的事实，形成企业与行业比较、分方向审批报告和综合风险判断。

当前代码已经具备企业画像、行业背景、历史案例比较和核心风险判断能力。下一阶段的重点不是增强 Agent，而是建立：

1. 企业领域与行业、同行基准的稳定关联；
2. 同行企业标准化指标矩阵；
3. 样本内排名和排名赋分；
4. 分方向审批点、方向总结和方向评价；
5. 综合审批报告。

## 3. 当前有效能力

- PDF、HTML 解析为 SourceAsset 和 EvidenceUnit；
- 企业与行业材料集合隔离；
- 企业十领域画像候选生成；
- 行业八维背景生成和全局语义审核；
- 字段、关系、维度、类型和连续原文摘录校验；
- 人工审核和 approved 正式画像；
- 历史企业案例分析；
- ComparisonCard、本地相似案例召回和详细比较；
- 自然语言核心风险判断与报告导出；
- 默认模型 `deepseek-v4-flash`；
- ReAct 按领域独立运行，证据发现使用 thinking，事实抽取使用 sampling；单个 ChatDeepSeek 请求有 180 秒超时；
- 授信审批指引 11 个方向、分方向报告、方向排名和综合核心风险判断；
- 最近一次完整回归测试为 `288 passed`；客户/供应商双侧分批规则和主题事实引用校验已纳入该结果。

## 4. 当前数据状态

- 当前 V5 工作数据库为 `data/current_project.db`；
- 该数据库保存宇树、埃夫特、科沃斯、云深处、乐聚智能、石头科技、越疆科技、深之蓝、天智航、亿嘉和、哈工智能、赛为智能共十二家企业画像，以及两份 2026 机器人产业研报对应的最新行业背景画像；
- 当前数据库 `data/current_project.db` 包含 14 个来源、10658 个证据单元、12 个已批准企业画像和 1 个已批准行业画像；十二家企业画像均完成六个主题分析维度。
- 已建立并批准 1 个十家企业的 2025 年智能机器人同行样本，已审核营业收入、经营活动现金流两项共 20 条指标值；企业画像和行业画像的证据引用已按 3200 字符新切片校验。
- “财务情况”已完成十家企业分方向报告、样本内方向排名和人工批准的真实闭环；哈工智能、赛为智能因报告期不一致，尚未进入该数值排名样本。
- 另建有 `robotics_2025_assumption_test` 十二家假设口径试验样本：哈工智能保留 2021 年原始事实、赛为智能保留 2024 年原始事实，仅按 2025 年口径参与流程测试。十二家试验版财务报告及其方向排名均保持 `pending`，不得与正式样本或正式审批结论混用。
- 历史数据库、旧上传副本和旧测试结果已从工作空间清理，不参与页面展示。

## 5. 当前代码入口

```text
v5_app.py
src/ui/v5_services.py
src/sources/
src/evidence/
src/profiles/
src/industry/
src/case_analysis/
src/ontology/
ontology/
prompts/
```

运行：

```powershell
streamlit run v5_app.py
python -m pytest -q
```

## 6. 下一步先完成的业务设计

后续扩展或调整代码前，仍需先确认以下业务内容：

1. 企业十领域继续保留，还是合并成更少的审批方向；
2. 每个审批方向包含哪些固定审批点；
3. 每个审批点引用哪些行业维度；
4. 每个审批点使用哪些同行可比指标；
5. 哪些指标越高越好、越低越好、按阶段比较或只用于解释；
6. 同行样本选择规则和报告期；
7. 缺失值、并列名次、有效样本数和排名赋分规则；
8. 方向排名如何汇总为综合结论。

这些内容应先形成“审批方向—审批点—行业基准—同行指标—排名规则”总表。

## 7. 下一阶段实施顺序

### 阶段一：Schema 与规则（已完成）

- 定义 `PeerCohort`；
- 定义 `ComparableMetric`；
- 定义 `DomainApprovalReport` 和 `ApprovalPoint`；
- 建立企业领域与行业维度映射；
- 固化排名方向、缺失值、并列和赋分规则。

### 阶段二：同行企业数据（首轮测试已完成）

- 导入约十家同行企业年报；
- 批量生成企业十领域标准化事实；
- 统一报告期、单位、合并范围和指标口径；
- 形成人工可检查的企业指标矩阵。

当前真实材料执行时，先建立“2025 年智能机器人同行样本”：宇树、埃夫特、科沃斯、云深处、乐聚智能、石头科技、越疆科技、深之蓝、天智航、亿嘉和均有 2025 年可比财务事实。哈工智能当前材料为 2021 年报、赛为智能当前材料为 2024 年报，不能与该样本混合做数值排名；两家保留企业画像，待补齐 2025 年材料后加入同期样本，或仅生成不含样本排名的单企业审批报告。

本轮真实验证已完成该样本的“财务情况”方向：使用已批准的行业背景、企业事实、营业收入和经营活动现金流两项样本内指标，分别生成十家企业报告并完成横向排名。研发费用率只作为企业事实解释，不因“数值越高”而自动赋予更高名次。下一步按同一流程验证技术实力、行业市场空间等代表性方向，再扩展其余方向。

### 阶段三：排名与审批报告（已完成首轮闭环）

- Python 计算样本内名次和排名赋分；
- 组合企业现状、行业背景和同行排名；
- 生成各审批点评价和一句话方向总结；
- 增加人工审核、页面展示和导出。

### 阶段四：综合报告（已完成首轮闭环）

- 汇总已批准的方向报告；
- 生成核心优势、风险、不确定性和核实事项；
- 明确样本范围和证据缺口；
- 输出最终综合审批报告。

## 8. 实施约束

- 每次代码变更前先在主技术手册写清需求、实现步骤和验收标准；
- 函数命名清楚，流程保持简单，不增加无业务必要的冗余检查；
- 模型不计算排名，Python 不补写企业事实；
- 未披露数据不等于零，不自动进入末位；
- 只表述样本内排名，不夸大为全行业排名；
- 表现得分与证据充分度分开；
- 画像、同行样本、排名和报告均保留人工审核门禁；
- 自动化测试不得调用真实付费模型。

## 9. 完成下一阶段的判定标准

- 至少一个审批方向能够关联企业事实、行业背景和同行指标；
- 同一批固定输入可重复得到相同排名；
- 报告显示原始值、有效样本数、名次和排名赋分；
- 缺失值不会被计算为零或最后一名；
- 每个审批点有企业现状、基准、排名、评价、证据和信息缺口；
- 方向总结不超出审批点；
- 结果保持 `pending`，人工批准后才进入综合报告。

## 10. 当前画像质量复核（2026-08-10）

### 复核要求

逐家将正式画像中的关键事实与对应 PDF 原文核对，优先检查：

1. 近三年收入、净利润、经营现金流和研发投入的年度、单位与口径；
2. 专利申请、授权、有效专利及其分项数量是否混淆；
3. 核心技术名称、技术来源、成熟阶段和技术储备是否完整；
4. 客户、供应商的角色、集中度、关联关系和交易期间是否错配；
5. 画像分析是否覆盖已提取事实，且明确披露范围和信息缺口。

### 实施步骤

1. 建立十家企业的 PDF 来源、EvidenceUnit、画像字段和主题分析清单；
2. 对确定性错误和缺失事实进行有证据引用的数据库修正，保留原始页码与摘录；
3. 对受影响主题重新执行分析，确保摘要、事实引用和信息边界同步更新；
4. 运行结构校验和测试，复核所有引用可回溯到当前 PDF，最后记录修正结果。

## 11. 新增企业画像提取：哈工智能与赛为智能（2026-08-11）

### 需求范围

本次只新增两家企业，不删除或重跑已有企业：

- `哈工智能2021年报.pdf`：画像报告期按 PDF 实际披露年份记录为 2021 年；
- `赛为智能2024年报.pdf`：画像报告期按 PDF 实际披露年份记录为 2024 年。

两份年报均按当前企业画像的统一六个主题提取：基本信息与经营范围、治理与团队、财务与经营、产品与商业化、技术与知识产权、客户供应商与风险。事实必须引用本次 PDF 的 EvidenceUnit，未披露内容明确标记为信息缺口，不用模型臆测补齐。

企业发展阶段只有在原文明确使用“初创、成长、成熟、转型”等阶段性表述时才可作为结构化事实；不得由成立年限、上市年限或业务描述推断。专利、软件著作权、商标等知识产权类别必须分开记录：软件著作权数量不得写入专利申请或专利授权字段，专利数量必须保留发明、实用新型、外观设计、总量或新增量等原文统计范围。

### 实现步骤

1. 检查两份 PDF 文件和数据库中的来源记录，确定稳定的企业 case_id，避免重复导入；
2. 使用当前 3200 字符连续切片规则导入 PDF 和 EvidenceUnit，保留页码、标题和原文摘录；
3. 使用当前事实结构化流程提取原子事实，再按六个主题归并为企业画像；事实抽取使用 sampling，主题分析使用 DeepSeek thinking；
4. 对两家企业分别运行六个主题的画像分析，摘要必须基于已提取事实并保留证据引用、披露边界和信息缺口；
5. 只验收新增企业：核对企业名称、报告期、财务单位与年度、专利口径、核心技术完整性、客户供应商角色和所有证据引用；
6. 运行结构校验和自动化测试，确认已有企业画像、来源和行业背景数据未被修改。

### 验收标准

- 新增两个来源，已有来源数量和内容不变；
- 两家企业各有一份可追溯到本企业 PDF 的完整画像；
- 六个主题均有分析结果或明确的信息缺口，不能出现占位名称；
- 2021/2024 报告期、金额单位、专利申请/授权/有效口径不混淆；
- 画像事实、主题事实引用和来源 case_id 全部匹配；
- `pytest -q` 通过，新增流程不触发真实模型调用以外的测试副作用。
